# Dogar Trades Website

Static website mockup for Dogar Trades — premium renovations & maintenance, Whyalla SA.

## Deploy
This is a static site. The homepage is `index.html` at the root.
All media lives in `/assets`. No build step required.

On Vercel: import this repo, leave Framework Preset as "Other", no build command, output directory = root.
